# Keep this directory in the repo but never commit its contents.
# Runtime secrets (Web Push VAPID PEM keys) are placed here by `just bootstrap`
# or by hand on deploy, then mounted read-only into the backend containers.
# Ignore everything in this dir except this file itself.
*
!.gitignore
