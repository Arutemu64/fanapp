// Wildcard permission name shipped by the backend for roles that implicitly
// hold every permission (currently ORG). Matching it grants any permission
// check. Mirrors WILDCARD_PERMISSION in backend core/vo/permission.py.
export const WILDCARD_PERMISSION = '*';
