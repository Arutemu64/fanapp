# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
exclude: '^(\.agent|\.agents|\.claude)/'

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        # --unsafe checks YAML syntax without constructing custom tags, which
        # docker-compose uses (!reset / !override for merge control).
        args: [--unsafe]
      - id: check-added-large-files

  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Matches the version of Ruff installed in your environment
    rev: v0.15.14
    hooks:
      # Linter + formatter cover backend/ and scripts/. No --config: ruff's
      # config discovery resolves backend/ files to backend/ruff.toml and
      # scripts/ to the repo-root ruff.toml (which extends the backend rules).
      # An explicit --config would break per-file-ignores, which ruff matches
      # relative to the cwd.
      - id: ruff-check
        args: [ --fix ]
        files: ^(backend|scripts)/
      - id: ruff-format
        files: ^(backend|scripts)/

  - repo: local
    hooks:
      - id: ty
        name: ty
        entry: uv run --directory backend ty check src/fanfan
        language: system
        types: [ python ]
        pass_filenames: false

      - id: frontend-prettier
        name: frontend prettier
        entry: bash -c 'cd frontend && pnpm exec prettier --check "${@#frontend/}"' --
        language: system
        files: ^frontend/
        types_or: [ svelte, ts, javascript, css, json ]

      - id: frontend-eslint
        name: frontend eslint
        entry: bash -c 'cd frontend && pnpm exec eslint "${@#frontend/}"' --
        language: system
        files: ^frontend/
        types_or: [ svelte, ts, javascript ]

      - id: frontend-check
        name: frontend svelte-check
        entry: bash -c 'cd frontend && pnpm check'
        language: system
        files: ^frontend/
        types_or: [ svelte, ts ]
        pass_filenames: false
